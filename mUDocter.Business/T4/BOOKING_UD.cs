/*
namespace mUDocter.Business    
{    
    /// <summary>
    /// A class which represents the BOOKING_UD table in the Main Database.
    /// </summary>
    public class BOOKING_UD
    {
               

        
        public int id {get; set; }
        
        public int? patient_id {get; set; }
        
        public int? docter_id {get; set; }
        
        public DateTime? time_in {get; set; }
        
        public double? longitude {get; set; }
        
        public double? latitude {get; set; }
        
        public string comment {get; set; }
        
        public DateTime created {get; set; }
        
        public int status {get; set; }
        
        public DateTime updated {get; set; }
        
        public int? tam_date {get; set; }
        
        public int? vs_date {get; set; }
        
        public double rate {get; set; }
        
        public string date_time {get; set; }
             

    } 
}

*/
/*
columns: [	{ field: "id", title: getColumnString('id') },
		{ field: "patient_id", title: getColumnString('patient_id') },
		{ field: "docter_id", title: getColumnString('docter_id') },
		{ field: "time_in", title: getColumnString('time_in') },
		{ field: "longitude", title: getColumnString('longitude') },
		{ field: "latitude", title: getColumnString('latitude') },
		{ field: "comment", title: getColumnString('comment') },
		{ field: "created", title: getColumnString('created') },
		{ field: "status", title: getColumnString('status') },
		{ field: "updated", title: getColumnString('updated') },
		{ field: "tam_date", title: getColumnString('tam_date') },
		{ field: "vs_date", title: getColumnString('vs_date') },
		{ field: "rate", title: getColumnString('rate') },
		{ field: "date_time", title: getColumnString('date_time') },
	}],




	$("#id").val(dataItem.id);
	$("#patient_id").val(dataItem.patient_id);
	$("#docter_id").val(dataItem.docter_id);
	$("#time_in").val(dataItem.time_in);
	$("#longitude").val(dataItem.longitude);
	$("#latitude").val(dataItem.latitude);
	$("#comment").val(dataItem.comment);
	$("#created").val(dataItem.created);
	$("#status").val(dataItem.status);
	$("#updated").val(dataItem.updated);
	$("#tam_date").val(dataItem.tam_date);
	$("#vs_date").val(dataItem.vs_date);
	$("#rate").val(dataItem.rate);
	$("#date_time").val(dataItem.date_time);



*/
